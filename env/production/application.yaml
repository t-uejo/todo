spring:
  application:
    name: todo

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  datasource:
    url: jdbc:postgresql://postgres-production:5435/todo
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
